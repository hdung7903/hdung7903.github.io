const prefix = 'src/assets/images/all-icon/';
const pngSuffix = '.png';

const icon = {
    email: `${prefix}email${pngSuffix}`,
    lock: `${prefix}lock${pngSuffix}`,
    ggIcon: `${prefix}ggIcon${pngSuffix}`,
    fbIcon: `${prefix}fbIcon${pngSuffix}`,
};

export default icon;
